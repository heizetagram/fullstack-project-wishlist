package repositories;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.jdbc.core.BeanPropertyRowMapper;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.jdbc.core.RowMapper;
import org.springframework.stereotype.Repository;

@Repository
public class UserRepository {
    /*
    @Autowired
    JdbcTemplate jdbcTemplate;
    public void addUser(int userId, String firstName, String lastName, String email, String user_password); {
        String query = "Insert INTO user(int userId, String firstName, String lastName, String email, String user_password)" +
                "VALUES (?, ?, ?, ?, ?)";
        jdbcTemplate.update()
    }

     */
}

